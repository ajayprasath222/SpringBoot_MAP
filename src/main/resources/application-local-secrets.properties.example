# Local secrets — copy to application-local-secrets.properties (gitignored)

POSTGRES_LOCAL_PASSWORD=

# --- Brevo API (optional — send OTP to real inbox locally) ---
#brevo.api-key=your-brevo-api-key
#app.mail.from=your-verified@gmail.com
#app.mail.delivery=brevo

# Without Brevo: embedded mode (OTP in data.SendOtp.otp and GET /api/v1/dev/mails)
